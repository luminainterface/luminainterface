# LLM Integration Requirements
# This file contains dependencies for the LLM integration with the V5 system

# OpenAI API client
openai>=1.0.0

# Anthropic API client
anthropic>=0.5.0

# Core requirements
pyside6>=6.0.0
numpy>=1.20.0
pandas>=1.3.0
requests>=2.25.0
python-dotenv>=0.19.0

# For threading and async processing
asyncio>=3.4.3

# Optional: Sentence transformers for embedding (if not using OpenAI embeddings)
sentence-transformers>=2.2.0 